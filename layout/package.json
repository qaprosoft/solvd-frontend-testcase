{
  "name": "layout",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "parcel watch src/**/*.pug",
    "dev": "browser-sync start -s ./dist -f ./dist/*",
    "build": "rm -rf ./dist | parcel build src/**/*.pug --no-content-hash --no-source-maps --no-minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.26.13",
    "parcel": "^1.12.4"
  },
  "devDependencies": {
    "pug": "^3.0.0",
    "sass": "^1.29.0"
  }
}
